# 如何讓不重要的 feat/fix 不出現在 Changelog 中

## 📌 方法總覽

有三種主要方法可以處理不重要的更新：

---

## ✅ 方法一：改用其他 Commit 類型（最簡單，推薦）

### 原理
只有 `feat`、`fix`、`perf`、`revert` 會出現在 changelog 中，其他類型都不會。

### 使用方式

```bash
# 不重要的新功能 → 用 chore 代替 feat
git commit -m "chore: 調整按鈕顏色"
git commit -m "chore: 更新默認配置"

# 不重要的修復 → 用 chore 或 style 代替 fix
git commit -m "chore: 修復拼寫錯誤"
git commit -m "style: 調整代碼縮排"

# 重要的更新 → 正常使用 feat/fix
git commit -m "feat: 新增用戶登入功能"
git commit -m "fix: 修復資料丟失問題"
```

### 類型選擇建議

| 情況 | 原本想用 | 改用 | 說明 |
|------|---------|------|------|
| 小調整、配置更新 | feat | **chore** | 不影響用戶的內部調整 |
| UI 微調、樣式修改 | feat/fix | **style** | 純視覺調整 |
| 拼寫錯誤、格式 | fix | **chore** | 不影響功能的小修正 |
| 代碼重構 | feat/fix | **refactor** | 內部優化 |
| 文檔更新 | feat | **docs** | 文檔相關 |

---

## ⚡ 方法二：使用 [skip-changelog] 標記（進階）

### 原理
在 commit message 中添加 `[skip-changelog]` 標記，使用自定義腳本過濾。

### 步驟 1：使用標記提交

```bash
# 在 subject 中添加標記
git commit -m "feat: 調整按鈕顏色 [skip-changelog]"
git commit -m "fix: 修復小的樣式問題 [skip-changelog]"

# 或在 body 中添加
git commit -m "feat: 更新配置

[skip-changelog]
這是一個不重要的內部調整"
```

### 步驟 2：使用自定義腳本生成 changelog

我已經為您創建了一個腳本 `scripts/generate-changelog.mjs`，它會：
1. 生成 changelog
2. 自動過濾掉包含 `[skip-changelog]` 的 commits

### 步驟 3：更新 package.json scripts

```json
{
  "scripts": {
    "changelog": "node scripts/generate-changelog.mjs",
    "changelog:all": "node scripts/generate-changelog.mjs --all"
  }
}
```

### 優點
- 可以使用正確的 commit 類型（feat/fix）保持語義準確性
- 靈活控制哪些 commits 出現在 changelog 中
- commitlint 不會報錯

### 缺點
- 需要額外的腳本
- commit message 會稍微長一點

---

## 🎯 方法三：使用 Scope 區分（推薦給大型項目）

### 原理
使用特定的 scope 來標記內部更新，然後配置過濾規則。

### 使用方式

```bash
# 內部更新使用特定 scope
git commit -m "feat(internal): 調整內部配置"
git commit -m "fix(internal): 修復內部邏輯"

# 重要更新使用正常 scope 或不加 scope
git commit -m "feat(auth): 新增登入功能"
git commit -m "fix(api): 修復資料獲取問題"
```

### 配置過濾（需要自定義腳本）

可以修改 `scripts/generate-changelog.mjs`，過濾掉特定 scope 的 commits。

---

## 💡 實際建議

### 對於個人/小型專案
**使用方法一**（改用其他類型）最簡單：
- 重要的 → `feat`、`fix`
- 不重要的 → `chore`、`style`

### 對於團隊/大型專案
**結合方法一和方法三**：
- 使用語義明確的 commit 類型
- 使用 scope 來區分重要程度
- 或使用 `[skip-changelog]` 標記

---

## 📝 範例對比

### ❌ 不好的做法
```bash
# 所有更新都用 feat/fix，導致 changelog 很亂
git commit -m "feat: 調整按鈕顏色"  # 不重要但會出現在 changelog
git commit -m "fix: 修復拼寫"      # 不重要但會出現在 changelog
```

### ✅ 好的做法（方法一）
```bash
# 根據重要程度選擇類型
git commit -m "chore: 調整按鈕顏色"     # 不會出現在 changelog
git commit -m "chore: 修復拼寫"        # 不會出現在 changelog
git commit -m "feat: 新增用戶登入功能"  # 會出現在 changelog ✨
git commit -m "fix: 修復資料丟失問題"   # 會出現在 changelog 🐛
```

### ✅ 好的做法（方法二）
```bash
# 使用標記
git commit -m "feat: 調整按鈕顏色 [skip-changelog]"
git commit -m "fix: 修復拼寫 [skip-changelog]"
git commit -m "feat: 新增用戶登入功能"  # 不加標記，會出現
```

---

## 🔧 如何切換方法

### 現在使用方法一（默認）
不需要任何配置，直接使用不同的 commit 類型即可。

### 想切換到方法二（使用標記）
1. 檢查 `scripts/generate-changelog.mjs` 是否存在
2. 更新 `package.json` 的 scripts：

```json
{
  "scripts": {
    "changelog": "node scripts/generate-changelog.mjs",
    "changelog:all": "node scripts/generate-changelog.mjs --all"
  }
}
```

3. 開始使用 `[skip-changelog]` 標記

---

## 🎯 總結

| 方法 | 難度 | 靈活性 | 推薦度 |
|------|------|--------|--------|
| 方法一：改用類型 | ⭐ 簡單 | ⭐⭐ 中等 | ⭐⭐⭐⭐⭐ |
| 方法二：使用標記 | ⭐⭐ 中等 | ⭐⭐⭐ 高 | ⭐⭐⭐⭐ |
| 方法三：使用 scope | ⭐⭐⭐ 複雜 | ⭐⭐⭐ 高 | ⭐⭐⭐ |

**建議**：先使用方法一，如果覺得不夠靈活，再考慮方法二。

---

有任何問題歡迎參考 [COMMIT_CONVENTION.md](./COMMIT_CONVENTION.md)！

